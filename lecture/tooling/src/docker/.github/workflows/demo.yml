name: demo
on:
    push:
        branches: [master]
    workflow_dispatch:

jobs:
    job1:
        runs-on: ubuntu-latest
        container: eclipse-temurin:25-jdk
        steps:
            - uses: actions/checkout@v7
            - run: java -version
            - run: javac Hello.java
            - run: java Hello
            - run: ls -lags
