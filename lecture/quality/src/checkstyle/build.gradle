plugins {
    id "java"
    id "application"
    id "checkstyle"
}

repositories {
    mavenCentral()
}

dependencies {
}

checkstyle {
    configFile file('checkstyle.xml')
    toolVersion '10.19.0'
}

application {
    mainClass = 'wuppie.deeplearning.strategy.MyWuppieStudi'
}
