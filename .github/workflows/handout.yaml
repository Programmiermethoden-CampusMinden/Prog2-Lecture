name: Handout (Docsify plus PDF)

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  call:
    uses: ./.github/workflows/_build-materials.yaml
    with:
      mktarget: handout
      metadata: pm.yaml
      build_dir: _s26
